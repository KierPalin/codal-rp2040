cmake_minimum_required(VERSION 3.13)
project( codal-rp2040 )
enable_language(ASM)

# Setup the Pico SDK
set(PICO_SDK_PATH "${CMAKE_CURRENT_SOURCE_DIR}/pico-sdk")
set(PICO_PLATFORM "rp2040")
set(PICO_TARGET "PICO")
set(PICO_CONFIG_HEADER_FILES "${CMAKE_CURRENT_SOURCE_DIR}/inc/sdk_config.h")

include(pico-util.cmake)

include(${PICO_SDK_PATH}/pico_sdk_init.cmake)
pico_sdk_init()


#CODAL:
include("${CODAL_UTILS_LOCATION}")

# Set the include directories for the HAL
set(HAL_RP2040_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/pico-sdk/src/rp2040")
set(HAL_RP2_COMMON_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/pico-sdk/src/rp2_common")
set(HAL_COMMON_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/pico-sdk/src/common")
set(HAL_HOST_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/pico-sdk/src/host")

set(HAL_STRUCTS_RP2040  "${PICO_SDK_PATH}/src/rp2040/hardware_structs/include")
set(HAL_STRUCTS_RP2     "${PICO_SDK_PATH}/src/rp2_common/hardware_structs/include")
set(HAL_REGS_RP2040     "${PICO_SDK_PATH}/src/rp2040/hardware_regs/include")
set(HAL_REGS_RP2        "${PICO_SDK_PATH}/src/rp2_common/hardware_regs/include")
set(PICO_BASE_HDRS      "${PICO_SDK_PATH}/src/common/pico_base_headers/include")
set(RP2_COMMON_INC      "${PICO_SDK_PATH}/src/rp2_common/include")
set(CMSIS_DEVICE_INCLUDE "${PICO_SDK_PATH}/src/rp2_common/cmsis/stub/CMSIS/Device/RP2040/Include")
set(CMSIS_INCLUDE        "${PICO_SDK_PATH}/src/rp2_common/cmsis/stub/CMSIS/Include")


RECURSIVE_FIND_DIR(PROCESSOR_DIR_INCLUDES "${PROCESSOR_INCLUDE_ROOT}", "*.h")
RECURSIVE_FIND_DIR(HAL_DIR_INCLUDES "${PROCESSOR_INCLUDES}", "*.h")
RECURSIVE_FIND_DIR(TOP_LEVEL_INCLUDE_DIRS "${CMAKE_CURRENT_SOURCE_DIR}/inc" "*.h")
RECURSIVE_FIND_FILE(TOP_LEVEL_SOURCE0 "${CMAKE_CURRENT_SOURCE_DIR}/source" "*.cpp")
RECURSIVE_FIND_FILE(TOP_LEVEL_SOURCE1 "${CMAKE_CURRENT_SOURCE_DIR}/source" "*.c")

list(APPEND TOP_LEVEL_SOURCE0 "asm/CortexContextSwitch.s")


# Include everyhing in pico-sdk/src/common/pico_base_headers/include/
set(HAL_INCLUDES
    ${HAL_RP2040_ROOT}/include
    ${HAL_RP2_COMMON_ROOT}/include
    ${HAL_COMMON_ROOT}/include
    ${HAL_HOST_ROOT}/include
    ${CMAKE_CURRENT_SOURCE_DIR}/inc

    ${HAL_STRUCTS_RP2040}
    ${HAL_STRUCTS_RP2}
    ${HAL_REGS_RP2040}
    ${HAL_REGS_RP2}
    ${PICO_BASE_HDRS}
    ${RP2_COMMON_INC}

    ${CMAKE_CURRENT_SOURCE_DIR}/inc
)


# set(PICO_DEFAULT_BOOT_STAGE2 "boot2_w25q080") # second stage bootloader, read from flash chip

# Causes CMake error due to 'Files generated by multiple commands.'
# include("${HAL_COMMON_ROOT}/pico_base_headers/generate_config_header.cmake")

# add_subdirectory("${HAL_COMMON_ROOT}/pico_base_headers")
# add_subdirectory(${HAL_RP2_COMMON_ROOT})
# add_subdirectory(${HAL_RP2040_ROOT})

set(TARGET_DEPS
    cmsis_core
    hardware_sync
    hardware_irq
    hardware_gpio
    hardware_spi
    hardware_i2c
    hardware_uart
    hardware_pwm
    hardware_flash
    hardware_pio
    hardware_dma
    hardware_irq
    hardware_regs
    hardware_structs
    hardware_exception
    hardware_divider
    pico_runtime
    pico_float
    pico_double
)

message(STATUS "HAL_INCLUDES: ${HAL_INCLUDES}")
# include_directories(${HAL_INCLUDES})

# create our target
add_library(codal-rp2040 ${TOP_LEVEL_SOURCE} ${TOP_LEVEL_SOURCE0} ${TOP_LEVEL_SOURCE1} ${HAL_SOURCE})
# target_link_libraries(codal-rp2040 pico_base_headers)
# target_link_libraries(codal-rp2040 codal-core ${OBJECT_FILES} ${ARCHIVE_FILES} ${TARGET_DEPS} pico_stdlib)


target_link_libraries(codal-rp2040
  PUBLIC
    pico_base_headers
    pico_stdlib
    hardware_structs
    hardware_regs
    hardware_gpio
    hardware_irq
  PRIVATE
    codal-core
    ${OBJECT_FILES}
    ${ARCHIVE_FILES}
    ${TARGET_DEPS}
)



#set_target_properties(codal-samd PROPERTIES LINKER_LANGUAGE CXX)

message("${PROCESSOR_DIR_INCLUDES} ${TOP_LEVEL_INCLUDE_DIRS}")

# get_target_property(PICO_BASE_HEADERS_INCLUDES pico_base_headers INTERFACE_INCLUDE_DIRECTORIES)
#
# # expose it to parent cmake.
# target_include_directories(codal-rp2040 PUBLIC
#   ${PICO_BASE_HEADERS_INCLUDES}
#   ${HAL_INCLUDES}
#   ${TOP_LEVEL_INCLUDE_DIRS}
#   ${PROCESSOR_DIR_INCLUDES}
# )


# target_compile_options(codal-rp2040 PUBLIC
#   "-include" "${PICO_SDK_PATH}/src/rp2_common/hardware_structs/include/hardware/structs/io_irq_ctrl.h"
#   "-include" "${PICO_SDK_PATH}/src/rp2_common/hardware_structs/include/hardware/structs/iobank0.h"
# )


target_include_directories(codal-rp2040 PUBLIC
  # generated config + pico_base headers
  $<TARGET_PROPERTY:pico_base_headers,INTERFACE_INCLUDE_DIRECTORIES>

  # CMSIS device/includes (needed for RP2040.h)
  "${CMSIS_DEVICE_INCLUDE}"
  "${CMSIS_INCLUDE}"

  # make the SDK's hardware_structs / hardware_regs interface directories absolutely explicit
  $<TARGET_PROPERTY:hardware_structs,INTERFACE_INCLUDE_DIRECTORIES>
  $<TARGET_PROPERTY:hardware_regs,INTERFACE_INCLUDE_DIRECTORIES>

  # also pull in device-specific and peripheral interface dirs
  $<TARGET_PROPERTY:hardware_gpio,INTERFACE_INCLUDE_DIRECTORIES>
  $<TARGET_PROPERTY:hardware_irq,INTERFACE_INCLUDE_DIRECTORIES>

  # finally your fallback/manual SDK paths (keeps previous behavior)
  ${HAL_INCLUDES}
  ${TOP_LEVEL_INCLUDE_DIRS}
  ${PROCESSOR_DIR_INCLUDES}
)




get_target_property(_inc pico_base_headers INTERFACE_INCLUDE_DIRECTORIES)
message(STATUS "pico_base_headers includes: ${_inc}")
